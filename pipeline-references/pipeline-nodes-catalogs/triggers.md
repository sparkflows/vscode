# Trigger Nodes

## DAG Triggers
- **Trigger Next DAG** `07-TriggerNextDag/TriggerDagRun.json` — This node triggers the next pipeline to run.
- **Trigger Next Pipeline** `12-TriggerNextPipeline/triggerNextPipeline.json` — This node triggers the next pipeline to run.

## Workflow
- **Workflow** `08-Workflow/workflow.json` — This node allows user to select the list of workflows in the project to create a pipeline.
